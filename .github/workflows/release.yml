name: Release

on:
  push:
    tags:
      - 'v.*'

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Package English outputs
        run: |
          zip -j lonelog-en-${{ github.ref_name }}.zip \
            output/en/lonelog*.pdf output/en/lonelog*.epub output/en/lonelog*.odt output/en/lonelog*.md

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          name: "Lonelog ${{ github.ref_name }}"
          generate_release_notes: true
          files: |
            lonelog-en-${{ github.ref_name }}.zip
